# K230 第一版低延迟优化修改说明

> 项目：基于 K230 的流媒体服务器与 IPC 多进程优化  
> 本轮原则：只修改最有机会降低延迟的三处代码，并修复已发现的小错误；不进行大规模架构调整。  
> 修改日期：2026-07-15

## 1. 本轮修改结论

本轮共完成三项低延迟优化和两项小型正确性修复：

1. 小核读取 DATAFIFO 时主动丢弃积压旧帧，仅保留较新的待发送帧，避免延迟随着队列积压持续增加。
2. 降低逐帧日志频率，减少串口输出和格式化打印对实时发送线程的干扰。
3. 将 RTP 发送提前到快照解析之前，保证直播帧优先发送。
4. 将 OSD 持续时间常量由八进制 `0400U` 修正为十进制 `400U`。
5. 修复 `READ_DONE` 后继续读取 DATAFIFO 消息内容的潜在悬空访问问题。

这版修改主要针对小核队列等待、热路径日志以及快照处理阻塞 RTP 三类延迟。它不会直接消除播放器自身缓存，因此上板测试时还需要同时使用低缓存播放参数。

## 2. 修改一：DATAFIFO 积压帧清理

### 2.1 修改文件

- `small_core/lnc/nalu_datafifo.h`
- `small_core/src/nalu_datafifo.c`
- `small_core/src/RTSP_Create.c`

### 2.2 修改内容

新增 DATAFIFO 可读长度查询接口：

```c
int nalu_datafifo_get_avail_read_len(nalu_datafifo_reader_t *reader,
                                     k_u32 *read_len);
```

RTP 发送线程在读取正常帧之前检查当前积压数量。当队列至少存在两个消息项时，最多丢弃 `可读项数 - 1` 个旧项，使发送线程尽快靠近最新帧。

只有同时满足以下条件才允许清理积压：

- RTSP 客户端已经进入 PLAY 状态；
- RTP 目标地址有效；
- 客户端已经取得 VPS、SPS、PPS；
- 本地已缓存完整的 VPS、SPS、PPS；
- DATAFIFO 中至少有两个可读项。

### 2.3 安全约束

- 带 `MPP_NALU_IPC_FLAG_SNAPSHOT` 标志的快照帧不会被丢弃。
- 每个被丢弃的普通帧都执行 `DATAFIFO_CMD_READ_DONE`，防止大核缓冲区不能释放。
- 如果旧帧的 `READ_DONE` 失败，不继续丢弃该项，而是把它交回正常处理路径再重试一次。
- 发生丢帧后设置 `wait_for_idr = 1`，等待新的 IDR 再恢复 RTP 输出，避免从缺少参考帧的 P 帧开始播放而产生花屏。
- 丢帧日志采用低频输出，不在每一帧打印。

### 2.4 预期效果

15 FPS 下每帧间隔约为 66.7 ms。原队列深度为 3 时，旧帧排队理论上可能贡献约 67～200 ms 延迟。本修改的目标是阻止该部分延迟长期累积。

运行时出现以下日志表示清理逻辑已触发：

```text
[datafifo] low-latency drop old=N total_drop=M, wait for IDR
```

## 3. 修改二：减少发送热路径逐帧日志

### 3.1 修改文件

- `small_core/src/RTSP_Create.c`

### 3.2 修改内容

正常复制、正常发送的帧不再因为 `should_copy` 为真而强制逐帧打印。只有出现以下情况时才立即输出详细日志：

- 消息无效或帧序号回退；
- 帧复制失败；
- `READ_DONE` 失败；
- 帧复制时间超过 5 ms；
- `READ_DONE` 前处理超过 10 ms；
- `READ_DONE` 本身超过 5 ms；
- 当前帧是快照帧；
- RTP 发送失败或耗时异常。

没有客户端播放时的 `no copy needed` 日志调整为每 30 帧输出一次。

### 3.3 预期效果

减少 `printf` 格式化、串口锁竞争和串口发送对小核实时线程的占用，降低偶发抖动。该项通常不是 600 ms 延迟的唯一原因，但修改风险较低，并能避免日志放大队列积压。

## 4. 修改三：RTP 优先于快照解析

### 4.1 修改文件

- `small_core/src/RTSP_Create.c`

### 4.2 修改前后的顺序

修改前的主要顺序：

```text
复制编码帧 → READ_DONE → 快照解析/缓存 → RTP 发送 → 释放小核副本
```

修改后的顺序：

```text
复制编码帧 → READ_DONE → RTP 发送 → 快照解析/缓存 → 释放小核副本
```

### 4.3 逻辑闭环

RTP 发送和快照处理都使用已经复制到小核自有内存的 `owned_frame`，因此大核 DATAFIFO 缓冲区可以先通过 `READ_DONE` 释放。小核副本只在 RTP 和快照两个使用者都处理完之后释放，不会产生发送期间内存失效的问题。

### 4.4 预期效果

快照 NALU 扫描、参数集缓存和 JPEG 触发不再阻塞同一帧的 RTP 发送，可降低快照触发时的直播延迟尖峰。

## 5. 小错误修复

### 5.1 OSD 时间常量改为十进制

修改文件：`big_core/mpp_pipeline/motion_adapter.c`

```c
/* 修改前：前导 0 使该值按八进制解释，实际为十进制 256 */
#define MOTION_OSD_DURATION_MS  0400U

/* 修改后：实际为十进制 400 */
#define MOTION_OSD_DURATION_MS  400U
```

该错误主要影响 OSD 显示时长和事件时间判断，不是 600 ms 流媒体延迟的主要来源。

### 5.2 避免 READ_DONE 后访问消息内存

修改文件：`small_core/src/nalu_datafifo.c`

调用 `DATAFIFO_CMD_READ_DONE` 后，大核可能立即释放或复用对应缓冲区。因此现在会在调用前先保存：

```c
seq = msg->seq;
flags = msg->reserved;
```

后续日志只读取局部变量 `seq` 和 `flags`，不再解引用已经归还的 `msg`。

## 6. 逻辑闭环与卡死风险检查

| 检查项目 | 结果 | 说明 |
|---|---|---|
| 丢弃的普通帧是否归还 | 通过 | 每项均执行 `READ_DONE` |
| 快照帧是否可能被误丢 | 通过 | 检测快照标志后停止清理并进入正常处理 |
| 丢帧后是否直接发送依赖帧 | 通过 | 设置 `wait_for_idr`，等待 IDR 恢复 |
| 参数集未准备时是否清队列 | 通过 | 此时禁用积压清理 |
| `READ_DONE` 失败是否造成无限清理 | 通过 | 停止清理并转入正常路径重试 |
| RTP 与快照是否使用已释放内存 | 通过 | 两者都使用小核自有副本，最后统一释放 |
| 所有分支是否释放小核副本 | 通过 | 失败路径和正常路径均调用释放函数 |
| 是否新增互斥锁或线程等待 | 未新增 | 本轮没有新增锁、条件变量或阻塞等待 |
| C 文件与头文件花括号是否配对 | 通过 | 静态计数检查一致 |
| 是否完成目标 SDK 编译 | 未完成 | 当前电脑没有 K230 SDK/交叉编译环境 |

从静态代码路径看，本轮没有引入新的死锁环路，也没有发现新增的 DATAFIFO 项或小核帧副本泄漏路径。但“不会卡死”仍需通过开发板长时间运行确认，尤其要观察 UDP `sendto()` 在网络异常时是否长时间阻塞；本轮为控制改动范围，没有改成非阻塞发送。

## 7. 已完成的静态检查

本轮已检查以下内容：

- 新接口在头文件和源文件中均存在；
- 低延迟清理只在 PLAY、目标地址和参数集准备完成后启用；
- 快照项不会进入普通丢弃分支；
- 丢弃项执行 `READ_DONE`；
- 丢帧后等待 IDR；
- 调用顺序为 RTP、快照、释放；
- 普通帧不再逐帧打印详细日志；
- `READ_DONE` 后不再访问消息字段；
- OSD 常量为十进制 `400U`；
- 四个修改文件的花括号数量平衡。

静态检查结果为 10/10 通过。由于本机缺少 K230 交叉编译器和板端运行环境，尚未完成编译、烧录和实机压力测试。

## 8. 建议的上板验证步骤

### 8.1 首先确认功能闭环

1. 启动大小核程序并连接一个 RTSP 客户端。
2. 确认客户端可以取得 VPS、SPS、PPS 并正常解码。
3. 人为制造短暂网络拥塞，确认出现积压丢帧日志。
4. 确认丢帧后视频可在下一个 IDR 恢复，没有持续花屏。
5. 触发快照，确认快照仍能生成，直播流不停止。
6. 断开和重新连接 RTSP，确认线程仍可继续服务。

### 8.2 再测延迟

建议分别记录以下四组结果，每组至少测量 20 次：

| 组别 | 代码 | 播放器设置 | 用途 |
|---|---|---|---|
| A | 修改前 | 原设置 | 延迟基线 |
| B | 修改后 | 原设置 | 判断代码优化收益 |
| C | 修改前 | 低缓存设置 | 判断播放器缓存占比 |
| D | 修改后 | 低缓存设置 | 最终目标结果 |

若使用 FFplay，可采用低缓存参数进行对照测试：

```bash
ffplay -fflags nobuffer -flags low_delay -framedrop -analyzeduration 0 -probesize 32 rtsp://<K230-IP>:<PORT>/<PATH>
```

不同 FFmpeg 版本对 RTSP 传输参数的支持存在差异，应以板端和电脑端实际版本为准。

### 8.3 建议观察的日志与指标

- `total_drop` 是否缓慢增长，而不是每轮大量增长；
- 丢帧后等待 IDR 的时间；
- `copy_cost_ms`、`pre_done_cost_ms`、`read_done_cost_ms`；
- RTP 单帧发送耗时与发送失败次数；
- 快照触发时直播延迟是否出现尖峰；
- 连续运行 30 分钟以上是否出现无画面、内存增长或 DATAFIFO 停止推进。

## 9. 本轮未修改的风险点

为了避免一次改动过多，本轮暂未处理以下内容：

1. UDP `sendto()` 仍可能在极端网络异常时阻塞发送线程。
2. 播放器默认缓冲可能仍贡献数百毫秒延迟。
3. 编码器 GOP/IDR 周期会影响丢帧后的恢复速度。
4. 当前 RTP、快照仍在同一线程串行执行，只是调整了优先顺序。
5. 尚未加入贯穿大核采集、编码、IPC、小核发送和客户端显示的统一时间戳测量。

如果这版上板后仍明显高于 300 ms，下一轮应优先测量播放器缓存和 `sendto()` 阻塞时间，再决定是否进行非阻塞 UDP、缩短 GOP 或拆分快照线程，避免无依据地继续扩大修改范围。

## 10. 相关文档

- 完整延迟分析：`K230端到端延迟分析与优化建议.md`
- 本文档：`K230第一版低延迟优化修改说明.md`

