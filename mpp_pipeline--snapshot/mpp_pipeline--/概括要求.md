请直接阅读我工作区内的所有代码文件，然后**创建一个名为 PROJECT_OVERVIEW.md 的文件**，放在项目根目录下。

在这个 .md 文件中，用下面的结构来解释整个工程，确保我可以脱离终端、在任意 Markdown 阅读器里舒服地查看：

## 1. 函数视角的文件抽象
遍历项目里的核心文件（忽略配置、导入等非逻辑代码），把每个文件的主要职责抽象成一个或多个函数签名，按文件列出。
对每个函数，必须标明：
- 函数名（语义化，一看就知道干什么）
- 输入（参数类型与含义）
- 输出（返回值类型与含义）

格式示例：
### src/parser.py
- `parse_log_line(raw: str) -> dict`
  输入：单行原始日志字符串
  输出：解析后的结构化字段字典

## 2. 函数协作关系图
用**纯文本**画出调用链，例如：
`main() -> load_config() -> parse_log() -> save_report()`
并解释每一步传递了什么关键数据。不要用 Mermaid 语法，因为我要在纯文本编辑器中阅读。

## 3. 一次完整的数据旅行
假设一个最典型的场景（比如一次用户请求、一个文件处理任务），选一个具体的输入作为起点，逐步追踪：
- 它最先进入哪个函数？
- 该函数产出了什么？
- 产物又传给了哪个函数？
- ……一直追踪到最终的输出。
最后总结：整个工程对外到底输出了什么（新文件？打印结果？启动一个服务？）。

请确保最终生成的 PROJECT_OVERVIEW.md 格式干净、层次清晰，适合我离线慢慢看。